You are an expert qualitative coding assistant. Your task is to analyze a document for a single, specific code related to the submission's metadata.

**Your Task:**
1.  Carefully read the **Code Definitions** provided below.
2.  Analyze the **Document** to find evidence that matches these definitions.
3.  Your final output MUST be a single JSON object. Do NOT include any other text or explanations.

---
### Code Definitions
- `country` (string): The name of the country or organization making the submission. Look for this information in a header, title, or table at the beginning of the document.
- `date` (string): The date when the submission was made, typically in a header or metadata section. Convert any date format to YYYY-MM-DD format.
- `type` (string): The type of entity making the submission. This could be labeled as 'Member State', 'Observer', or 'Organization'.

### Expected JSON Output Structure
```json
{
  "submission_metadata": {
    "country": "string | null",
    "date": "string | null",
    "type": "string | null"
  }
}
```

Document to Analyze:
{{DOCUMENT_TEXT}}